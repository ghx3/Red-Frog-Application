<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:RedFrogs.Controls"
             NavigationPage.HasNavigationBar="False"
             x:Class="RedFrogs.Views.Login"
             BackgroundColor="White"> 
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackLayout Grid.Row="0" Grid.Column="0" BackgroundColor="#ffffff">
                <Image Source="logo.png" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" />
            </StackLayout>
            <StackLayout Grid.Row="1" Grid.Column="0">

                <Button ClassId="TLBtn" Margin="10, 0, 10, 0" TextColor="White" BorderRadius="10"
                               Clicked="Login_Clicked" BackgroundColor="#bf3336" Text="Log in as Team Leader" />

                <Button ClassId="UserBtn" Margin="10, 10, 10, 0" TextColor="White" BorderRadius="10"
                               Clicked="Login_Clicked" BackgroundColor="#bf3336" Text="Log in as User" />

            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>